[project]
name = "demo-python-pipeline"
version = "0.1.0"
description = "Data pipeline with Flask mock server, FastAPI ingestion, and PostgreSQL"
readme = "README.md"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["services*", "packages*"]