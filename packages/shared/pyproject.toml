[project]
name = "shared"
version = "0.1.0"
description = "Shared schemas and models"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.0"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"